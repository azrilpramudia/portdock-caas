
server {
    listen 80;
    server_name webwisata.co.id;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name webwisata.co.id;

    ssl_certificate /etc/letsencrypt/live/webwisata.co.id/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/webwisata.co.id/privkey.pem;

    location / {
        include /etc/nginx/portdock-apps/maintenance/status.conf;
        proxy_pass http://127.0.0.1:8001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
